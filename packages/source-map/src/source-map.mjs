import core from "./source-map.cjs";

const { GREATEST_LOWER_BOUND, LEAST_UPPER_BOUND, SourceMapConsumer, SourceMapGenerator } = core;

export { GREATEST_LOWER_BOUND, LEAST_UPPER_BOUND, SourceMapConsumer, SourceMapGenerator };
